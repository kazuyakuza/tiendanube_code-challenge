# Max Lines per Method Rule

- Methods/functions must be as short as possible to improve readability and testability.
- The body of a method/function mustn't exceed 50 lines.
- Count excludes method/function signature (name, parameters, return type, etc.).
- This rule may not apply in specific cases where a longer method is unavoidable.
