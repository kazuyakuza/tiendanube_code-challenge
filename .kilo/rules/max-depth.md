# Max Depth Rule

- The max indentation level within a file or method is limited to improve readability.
- As a general rule, the max depth of nested blocks mustn't exceed 2 levels.
- When 3rd level of nesting is required, extract the logic into a separate method or function.
- Indentation counts from the beginning of the file or the body of a method/function.
