# Max Arguments per Method Rule

- Methods and functions should not have more than 2 params.
- When required more than 2 params, encapsulate them in a class or object.
- When possible, define a type for the param object, and do it in a new file.
