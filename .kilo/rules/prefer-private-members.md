# Prefer Private Members Rule

- Members (properties, methods, etc.) must be defined as private by default.
- Set members as public when is absolutely necessary for the functionality of the app.
- This principle helps to encapsulate the internal logic of a class or module and reduce the public API surface.
